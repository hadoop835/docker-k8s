# --address=0.0.0.0: The IP address for the Kubelet to serve on (set to 0.0.0.0 for all interfaces)
NODE_ADDRESS="--address="
# --port=10250: The port for the Kubelet to serve on. Note that "kubectl logs" will not work if you set this flag.
NODE_PORT="--port=10250"
# --hostname-override="": If non-empty, will use this string as identification instead of the actual hostname.
NODE_HOSTNAME="--hostname-override="
# --api-servers=[]: List of Kubernetes API servers for publishing events,
# and reading pods and services. (ip:port), comma separated.
KUBELET_API_SERVER="--api-servers=https://:6443"
# DNS info
KUBELET_DNS_IP="--cluster-dns="
KUBELET_DNS_DOMAIN="--cluster-domain="
#kubelet pod infra container
KUBELET_POD_INFRA_CONTAINER="--pod-infra-container-image="
# Add your own!
KUBELET_ARGS="--kubeconfig=/kubeconfig.yaml"
